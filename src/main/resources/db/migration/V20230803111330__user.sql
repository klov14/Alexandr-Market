CREATE TABLE user1 (
                        id BIGSERIAL PRIMARY KEY,
                        first_name TEXT,
                        last_name TEXT,
                        email TEXT,
                        role TEXT,
                        password TEXT);
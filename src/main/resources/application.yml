spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: ${DATABASE_URL:jdbc:postgresql://localhost:5432/shop}
    username: postgres
    password: 12345678

#  jpa:
#    hibernate:
#      ddl-auto: create-drop
#    show-sql: true
#    properties:
#      hibernate:
#        format_sql: true
#      database: postgresql
#      database-platform: org.hibernate.dialect.PostgreSQLDialect
  flyway:
    url: ${DATABASE_URL:jdbc:postgresql://localhost:5432/shop}
    user: postgres
    password: 12345678
    enabled: true
    locations:
      - 'classpath:db/migration'
